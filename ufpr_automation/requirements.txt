# ============================================================================
# UFPR Automation — Requirements
# ============================================================================

# Automação de Navegador (OWA Web Scraping)
playwright>=1.49.0

# Gerenciamento de Variáveis de Ambiente (.env)
python-dotenv>=1.0.1

# RAG — Retrieval-Augmented Generation (Marco II)
pymupdf>=1.25.0
lancedb>=0.20.0
sentence-transformers>=4.0.0
langchain-text-splitters>=0.3.0,<1.0.0
pyarrow>=18.0.0

# ----------------------------------------------------------------------------
# Instalação do nanobot (Framework Core)
# O framework pai deve estar instalado. Se você estiver instalando
# em um novo ambiente, descomente a linha abaixo para instalá-lo
# diretamente do diretório pai em modo editável:
#
# -e ../
# ----------------------------------------------------------------------------
